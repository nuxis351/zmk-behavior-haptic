/ {
    behaviors {
        /omit-if-no-ref/ haptic: haptic {
            compatible = "zmk,behavior-haptic";
            #binding-cells = <0>;

            gpios = <&pro_micro 10 GPIO_ACTIVE_HIGH>;
            pulse-ms = <50>;
        };
    };
};
